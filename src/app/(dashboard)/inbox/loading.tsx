/**
 * Inbox Loading State
 * Shows skeleton UI while inbox data is loading
 */
import { PageSkeleton } from '@/components/ui/loading-skeleton';

export default function InboxLoading() {
  return <PageSkeleton />;
}
