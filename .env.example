# ============================================
# InboxAI - Environment Variables
# ============================================
# Copy file này thành .env.local và điền các giá trị
# cp .env.example .env.local
# ============================================

# --------------------------------------------
# SUPABASE (Bắt buộc)
# Lấy từ: https://supabase.com/dashboard/project/_/settings/api
# --------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --------------------------------------------
# AI APIs (Cần ít nhất 1)
# --------------------------------------------

# OpenAI (Khuyến nghị - dùng GPT-4o-mini)
# Lấy từ: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Anthropic Claude (Tùy chọn - dự phòng)
# Lấy từ: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# --------------------------------------------
# RESEND - Gửi Email (Bắt buộc)
# Lấy từ: https://resend.com/api-keys
# --------------------------------------------
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=noreply@your-domain.com

# Webhook secret (cho tracking email delivery)
# Tạo webhook tại: https://resend.com/webhooks
RESEND_WEBHOOK_SECRET=whsec_...

# --------------------------------------------
# GOOGLE OAuth (Tùy chọn - cho Gmail integration)
# Lấy từ: https://console.cloud.google.com/apis/credentials
# --------------------------------------------
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-...

# --------------------------------------------
# BẢO MẬT (Bắt buộc)
# --------------------------------------------

# Key mã hóa mật khẩu email IMAP (32 bytes hex)
# Tạo bằng: openssl rand -hex 32
ENCRYPTION_KEY=your-64-character-hex-string

# Secret cho CRON endpoints
# Tạo bằng: openssl rand -hex 32
CRON_SECRET=your-random-secret

# --------------------------------------------
# APP CONFIG
# --------------------------------------------

# URL của app (không có trailing slash)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Domain cho mailbox (nếu dùng custom domain với Resend)
NEXT_PUBLIC_MAILBOX_DOMAIN=your-domain.com

# --------------------------------------------
# CHẾ ĐỘ BẢO TRÌ (Tùy chọn)
# --------------------------------------------
MAINTENANCE_MODE=false

# --------------------------------------------
# SENTRY - Error Tracking (Tùy chọn)
# Lấy từ: https://sentry.io/
# --------------------------------------------
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# --------------------------------------------
# ANDROID BUILD (Chỉ cần khi build APK)
# --------------------------------------------
# KEYSTORE_PASSWORD=your-keystore-password
# KEY_ALIAS=your-key-alias
# KEY_PASSWORD=your-key-password
